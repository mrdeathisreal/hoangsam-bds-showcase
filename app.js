/*!
 * ============================================================
 * THIS FILE IS WITHHELD FROM THE PUBLIC SHOWCASE.
 * ============================================================
 * The full `app.js` — the controller that wires up the entire
 * Hoàng Sâm BĐS site — is part of the private source
 * repository.
 *
 * What you would see in the full file (~1370 lines):
 *   • Bootstrap: Firebase, auth, i18n, store, theme
 *   • Listings: filter, search, paginate, hydrate detail view
 *   • Admin: add / edit / delete / feature, listing form with
 *     image compression to ~150KB per photo
 *   • Lightbox: keyboard nav, zoom, swipe
 *   • Comments thread with moderation
 *   • AI assistant: chat overlay calling the Gemini Cloud
 *     Function with listing-context system prompt
 *   • Lead-intake submit: form validation, Cloud Function call,
 *     toast feedback
 *   • Robust DOM-ref handling and per-field error logging (FIX9)
 *
 * The modules it imports — under `src/` — are published here.
 * Read those to see the data layer, the rendering library, the
 * auth shim, and the i18n table. The entry point is what's
 * withheld.
 *
 * If you are evaluating this codebase for a hiring decision and
 * want to read the full file, contact:
 *
 *     nhhuy130@gmail.com
 *
 * See LICENSE — All Rights Reserved.
 * ============================================================
 */
console.warn(
  '[Hoàng Sâm BĐS] app.js is withheld from the public showcase. ' +
    'Contact nhhuy130@gmail.com for licensed access.',
);
